import React from "react";

const Home = () => {
    return (
        <div>
            <p>Hello!</p>
            <p>Try one of the menu items on the top-right navigation menu.</p>
        </div>
    );
};

export default Home;